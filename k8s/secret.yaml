# DEPRECATED — Original Todo API Secret (name: todo-api-secret, namespace: default).
# Superseded by kubecart-secret in k8s/demo/secret.yaml (which includes JWT_SECRET).
# Run: git rm k8s/secret.yaml
