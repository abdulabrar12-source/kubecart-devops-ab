# DEPRECATED — Todo API ClusterIP service (name: api-service, namespace: demo).
# Replaced by per-service Service manifests in k8s/kubecart/.
# Safe to delete this file once git mv k8s/demo k8s/kubecart is complete.

spec:
  type: ClusterIP
  selector:
    app: api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
